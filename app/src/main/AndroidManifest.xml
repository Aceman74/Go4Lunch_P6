<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.aceman.go4lunch">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".utils.OnCreate"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".navigation.activities.MainActivity"
            android:theme="@style/AppTheme.NoTitle">
            <!-- DISABLE TOOLBAR IN MAIN ACTIVITY-->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data
                android:name="com.google.android.geo.API_KEY"
                android:value="AIzaSyCyZRkdBfcTam6hkjWzst3N_Ms9EA5R1h8"/>
        </activity>
        <!-- PROFILE ACTIVITY -->
        <activity android:name=".auth.ProfileActivity"
            android:label="@string/toolbar_title_login_activity"
            android:parentActivityName=".navigation.activities.MainActivity"/>
        <!-- CORE ACTIVITY -->
        <activity android:name=".navigation.activities.CoreActivity"
            android:label="@string/toolbar_title_core_activity"
            android:theme="@style/AppTheme.Core"
            android:parentActivityName=".navigation.activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>
            <meta-data android:name="android.app.searchable"
                android:resource="@xml/searchable"/>
    </activity>
    </application>

</manifest>